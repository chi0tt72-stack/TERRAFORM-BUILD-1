# Shared variables for all Ansible hosts.
# Override ansible_ssh_private_key_file via -e or env (see README).
ansible_user: ec2-user
# Use the same key as for SSH to EC2 (e.g. from Terraform/Secrets Manager).
# ansible_ssh_private_key_file: "~/.ssh/terraform-course-key"
